"use strict";

import "jest";
import { TransformerLoader } from "../../src/framework/renderers/TransformerLoader";
import { Element, ElementTransformer } from "../../src/types/framework";

(function() {
    test("test rendering default page", () => {
    // jest.resetModules();
    // var tl: TransformerLoader = new TransformerLoader();
    // var et: ElementTransformer = tl.getTransormer("raw");
    // var element = { type: "dummy" } as Element;
    // console.log({ et });
    //et.transform(element);
    //expect((et as any).transformed).toBe("<!DOCTYPE html>");
    });
})();
